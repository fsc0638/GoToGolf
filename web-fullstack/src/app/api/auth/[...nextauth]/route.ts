import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({ message: "Auth scaffold placeholder. Enable provider integration next step." });
}

export async function POST() {
  return NextResponse.json({ message: "Auth scaffold placeholder. Enable provider integration next step." });
}
